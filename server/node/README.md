Optional Node host. Prefer `../php/` on shared hosting.

1. npm install express node-fetch@2
2. GEMINI_API_KEY=AIza... FIREBASE_PROJECT_ID=myjournal-plus node -e "
const http=require('http'); /* or copy logic from php — PHP is the maintained path */
console.log('Use server/php for production on typical web hosts');
"
